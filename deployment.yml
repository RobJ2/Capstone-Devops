apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: capstone-project
  labels:
    app: capstone-project
spec:
  strategy:
    type: RollingUpdate
  replicas: 2
  selector:
    matchLabels:
      app: capstone-project
  template:
    metadata:
      labels:
        app: capstone-project
    spec:
      containers:
        - name: capstone-project
          image: 04193007/capstone
          ports:
            - containerPort: 80